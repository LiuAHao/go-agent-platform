# Go Agent Platform 设计方案

## 1. 产品定位

Go Agent Platform 的目标不是做一个完全云端运行的 Agent 托管平台，而是做一个**本地优先的 Agent Studio**。

核心产品逻辑是：

- 云端负责统一提供 Skill、MCP、Agent 模板、客户端版本和资源更新
- 用户在桌面客户端中下载、安装和装配这些能力
- Agent 的主要运行、模型调用、文件访问和本地工具调用发生在用户本机
- Web 主要作为官网、资源市场浏览、登录注册和客户端下载入口
- 移动端后续作为通知、审批和轻量控制端

一句话概括：

**云端负责供给和管理，本地负责装配和执行。**

## 2. 为什么不做全云端运行

全云端运行在产品上看起来统一，但对当前项目并不合适。

主要原因：

- 本地文件、浏览器、命令行、数据库等资源天然更适合在本机访问
- 用户的 API Key、模型配置和私有连接不适合默认托管到云端
- 云端执行需要处理复杂的多租户隔离、权限控制、审计和资源成本
- 对 MVP 来说，云端执行会显著拖慢落地速度

因此，当前设计选择本地优先：

- 本地客户端承担 Agent 构建和执行
- 云端平台承担资源目录、分发、更新和同步
- 云端管理面板承担平台资源和运营管理

## 3. 产品形态与职责边界

### 3.1 桌面端

桌面端是主产品。

职责：

- 创建和管理 Agent
- 安装和管理 Skill
- 安装和管理 MCP
- 配置模型和 API Key
- 执行本地聊天和任务
- 后续接入本地文件、浏览器、命令行和本地数据库

当前建议优先使用 Electron，因为现有 React + Vite 前端可以直接复用。

### 3.2 云服务管理面板

云服务管理面板是平台运营侧使用的后台，不是普通用户的 Agent 工作台。

职责：

- 管理平台 Skill 上架、下架、版本和审核状态
- 管理平台 MCP 上架、下架、版本和审核状态
- 管理 Agent 模板、示例和推荐资源
- 管理用户、组织、权限和资源可见范围
- 查看资源下载、安装、调用失败等基础运营指标
- 管理公告、版本更新和客户端发布信息

设计原则：

- 管理面板只管理“平台供给的资源”和“平台运行状态”
- 不直接进入用户本地 Agent 的执行过程
- 不保存用户本地 API Key、私有文件路径或本地运行日志明细

### 3.3 云端 Registry 后端

云端 Registry 后端是资源分发与账号系统，面向 Web、桌面客户端和管理面板提供 API。

职责：

- 用户账号
- 平台 Skill 目录
- 平台 MCP 目录
- Agent 模板目录
- 资源版本管理
- 下载和安装索引
- 可选的配置同步
- 客户端版本检测
- 资源签名、校验和下载授权
- 管理面板 API
- 文档和示例

云端不默认承担用户任务的主执行。

### 3.4 Web

Web 是辅助入口。

职责：

- 官网展示
- 登录和注册
- 资源市场浏览
- 文档入口
- 桌面客户端下载

Web 不作为完整本地执行端。

### 3.5 客户端本地服务

桌面客户端内部需要一个本地服务层，可以是 Electron 主进程、独立本地 runtime 进程，或后续演进为本地守护进程。

职责：

- 保存本地 Agent、模型、Skill、MCP 和会话数据
- 管理本地 Skill 安装目录和版本
- 管理本地 MCP 配置、启动和连接测试
- 代理模型 API 调用，避免前端直接持有密钥
- 为 React 控制台提供本地 API
- 控制本地文件、浏览器、命令行等敏感能力的权限边界

设计原则：

- 前端只负责展示和交互，不直接操作本地系统能力
- 本地服务负责执行和权限校验
- 云端资源下载后必须进入本地资源库，再由 Agent 选择装配

### 3.6 移动端

移动端后续作为伴侣端，而不是完整工作台。

职责：

- 查看 Agent 状态
- 查看消息结果
- 接收通知
- 发起轻量任务
- 审批敏感操作

不建议在 MVP 阶段实现移动端。

## 4. 系统架构

### 4.1 总体结构

```text
Cloud Registry
  ├─ Auth
  ├─ Admin Console API
  ├─ Skill Marketplace
  ├─ MCP Marketplace
  ├─ Agent Template Registry
  ├─ Client Release
  └─ Distribution / Sync

Cloud Admin Console
  ├─ Resource Review
  ├─ Skill / MCP Version Management
  ├─ User / Organization Management
  └─ Operation Metrics

Web Portal
  ├─ Landing Page
  ├─ Login / Register
  ├─ Resource Browse
  └─ Desktop Download

Desktop Client
  ├─ React Console
  ├─ Electron Shell
  └─ Local Runtime Bridge / Local API

Local Runtime
  ├─ Agent Runtime
  ├─ Skill Runtime
  ├─ MCP Runtime
  ├─ Model Connector
  └─ Local Storage
```

### 4.2 云端与本地边界

云端适合做：

- 账号
- 市场
- 下载
- 更新
- 版本
- 同步
- 审核
- 用户和权限
- 资源运营指标
- 客户端发布管理

本地适合做：

- Agent 执行
- 模型调用
- 本地文件访问
- 本地浏览器自动化
- 本地命令执行
- 本地 MCP 调用
- 本地 Skill 运行
- 聊天记录和本地配置存储

### 4.3 推荐进程边界

MVP 阶段可以先采用一个 Go 后端进程同时承担本地 API 和云端 Registry API，以降低开发成本。但代码结构上必须先区分边界：

```text
internal/
  cloud/       # 云端账号、资源市场、管理后台 API
  local/       # 本地 Agent、Skill、MCP、模型、会话 API
  runtime/     # Agent 执行、Skill 执行、MCP 调用、模型调用
  shared/      # 通用领域对象、错误、鉴权、配置
```

后续部署时拆成：

```text
Cloud Backend
  └─ go-agent-platform server --mode cloud

Local Runtime
  └─ go-agent-platform server --mode local

Desktop Client
  └─ Electron + React Console
```

### 4.4 API 分层

建议按使用者拆分 API，而不是把所有接口混在一起：

- `cloud public api`：官网、资源市场、登录注册、客户端下载
- `cloud admin api`：平台资源审核、版本管理、用户管理、运营指标
- `desktop local api`：Agent 创建、聊天、模型配置、本地 Skill/MCP 管理
- `sync api`：资源安装、版本检查、可选配置同步

这样可以避免普通用户前端误用管理接口，也方便后续把云端和本地运行时分开部署。

### 4.5 安全边界

需要明确以下默认规则：

- 用户模型 API Key 默认只保存在本地，不上传云端
- 用户本地文件路径、命令执行记录和私有 MCP 配置默认只保存在本地
- 云端只保存平台资源元数据、下载地址、版本和用户安装关系
- 如果后续支持云同步，必须让用户显式开启，并区分可同步字段和禁止同步字段
- 平台 Skill / MCP 必须有签名、版本号和校验和，客户端安装前进行校验

## 5. 领域模型

当前核心模型：

- `User`：用户身份
- `Agent`：用户创建的智能体
- `Skill`：可安装、可绑定的能力包
- `Tool / MCP`：可调用的外部能力连接
- `Model`：模型配置
- `Session`：对话会话
- `Message`：会话消息
- `Task`：执行任务

需要补充的平台侧模型：

- `PlatformResource`：平台统一供给的资源抽象
- `ResourceVersion`：Skill / MCP / Agent 模板的版本信息
- `InstallRecord`：用户安装平台资源的记录
- `ReleaseChannel`：客户端发布通道
- `AuditRecord`：平台资源审核和管理操作记录

需要补充的本地侧模型：

- `LocalInstallation`：本地安装后的 Skill / MCP 实例
- `LocalSecret`：本地保存的模型 Key、MCP Token 等敏感配置
- `RuntimePermission`：Agent 调用本地能力时的权限边界
- `RuntimeEvent`：本地执行事件，用于 UI 展示和调试

资源关系：

```text
Platform Skill / MCP
        │
        ▼
My Skill / My MCP
        │
        ▼
Agent Binding
        │
        ▼
Chat / Task Execution
```

## 6. 用户流程

### 6.1 创建 Agent

1. 用户填写 Agent 名称
2. 用户填写 Agent 说明
3. 用户选择模型
4. 用户勾选已安装的 Skill / MCP
5. 创建后进入 Agent 聊天页

说明字段会同时写入：

- `description`
- `system_prompt`

### 6.2 安装 Skill

1. 用户浏览平台 Skill
2. 用户安装到“我的 Skill”
3. 用户创建 Agent 时勾选该 Skill
4. Agent 在执行时按配置调用

个人 Skill 支持两种方向：

- 本地上传文件夹
- AI 生成 Skill

MVP 阶段优先实现本地上传文件夹。

### 6.3 配置 MCP

MVP 默认只暴露：

- 名称
- 描述
- 绑定 JSON

高级设置包括：

- Schema
- 审批
- 发布范围

### 6.4 配置模型

MVP 默认只暴露：

- 模型名称
- 官方模型名称
- API URL
- API Key

高级设置包括：

- Provider
- 上下文窗口
- 最大输出 Token
- 默认模型

## 7. 当前实现状态

已经完成：

- Go 后端基础结构
- React + Vite 控制台
- 平台首页、登录页、注册页
- Agent 创建和聊天页面
- Skill / MCP / Model 管理页面
- Electron 基础桌面壳层
- Memory / PostgreSQL 存储实现
- MVP 实施计划文档

仍需补齐：

- 云服务管理面板的页面和后端接口
- 云端 Registry 与本地 Runtime 的 API 边界
- 本地 Skill 安装目录
- 本地 MCP 安装和启动管理
- 模型连通性测试
- MCP 连通性测试
- Skill 替换上传和更新
- 桌面端本地系统桥接能力

## 8. 建议补充的实现模块

### 8.1 云端管理面板

优先做最小可用后台，不做复杂数据大屏。

MVP 页面：

- 平台 Skill 管理：列表、创建、编辑、版本、启停
- 平台 MCP 管理：列表、创建、编辑、版本、启停
- Agent 模板管理：列表、创建、编辑、推荐状态
- 用户管理：列表、禁用、角色
- 客户端版本管理：版本号、下载地址、更新说明

### 8.2 云端后端

MVP API：

- 登录注册与用户信息
- 平台 Skill / MCP / Agent 模板列表
- 资源详情和版本信息
- 资源下载地址
- 用户安装记录
- 管理员资源管理接口

云端后端暂不做：

- 用户 Agent 云端执行
- 用户 API Key 托管
- 用户本地文件代理
- 大规模任务队列

### 8.3 桌面客户端

MVP 页面：

- 登录与绑定云账号
- 浏览平台 Skill / MCP
- 安装到我的 Skill / MCP
- 配置本地模型
- 创建 Agent 并绑定本地资源
- Agent 聊天页

客户端需要处理：

- 离线可用
- 本地数据迁移
- 本地资源目录管理
- 云端资源更新检查
- 敏感操作确认

### 8.4 本地运行时

本地运行时是后续真正形成产品壁垒的核心。

MVP 能力：

- 本地存储 Agent / Session / Message
- 使用本地模型配置发起模型调用
- 加载已安装 Skill 元数据
- 加载已配置 MCP 元数据
- 保存执行事件

后续能力：

- Skill 沙箱执行
- MCP 进程生命周期管理
- 本地文件访问权限控制
- 本地浏览器自动化
- 本地命令行执行审批
- 插件签名校验和运行隔离

## 9. MVP 目标

MVP 的目标是形成一个可演示的本地闭环：

1. 启动本地运行时
2. 启动桌面客户端
3. 登录或进入本地模式
4. 配置模型
5. 安装或创建 Skill / MCP
6. 创建 Agent
7. 进入聊天页
8. 发起对话并看到结果

MVP 不包含：

- 移动端
- 完整云同步
- 企业权限体系
- 计费系统
- 全云端任务运行
- 完整 Skill 在线编辑器

## 10. 实施阶段

### 阶段 1：桌面端基础客户端

目标：

- Electron 壳层可以启动
- 可以加载现有控制台
- 支持开发模式和生产模式运行

### 阶段 2：本地运行时边界

目标：

- 明确哪些 API 属于本地运行时
- 明确哪些 API 属于云端 Registry
- 为后续拆分代码和进程做准备

### 阶段 3：本地资源安装

目标：

- Skill 能下载到本地目录
- MCP 能保存为本地配置
- Agent 绑定本地可用资源

### 阶段 4：执行闭环

目标：

- Agent 能使用模型配置
- Agent 能读取已绑定 Skill / MCP
- 对话结果可以保存到本地

### 阶段 5：桌面能力增强

目标：

- 文件夹选择
- 本地目录管理
- 系统通知
- 自动更新
- 本地运行时守护进程

### 阶段 6：云端管理后台

目标：

- 管理平台 Skill / MCP / Agent 模板
- 发布资源版本
- 管理客户端下载地址
- 查看基础安装指标

### 阶段 7：云端 Registry 与客户端同步

目标：

- 客户端可以登录云端账号
- 客户端可以拉取平台资源目录
- 客户端可以下载资源并安装到本地
- 客户端可以检测资源更新

## 11. 风险与控制

### 风险 1：Web 继续承担主执行端

控制：

- Web 只承担入口、资源市场和文档职责
- 执行能力向桌面端和本地运行时收敛

### 风险 2：过早做移动端

控制：

- MVP 不做移动端
- 移动端只作为后续伴侣端规划

### 风险 3：过早实现全云端执行

控制：

- 云端只做资源分发
- 用户任务默认本地执行

### 风险 4：配置复杂度过高

控制：

- 默认页面只展示必要字段
- 专业配置放进高级设置
- 先保证普通用户可以完成闭环

### 风险 5：云端管理后台过早膨胀

控制：

- 管理后台 MVP 只做资源 CRUD、版本、启停和用户管理
- 不做复杂运营大屏
- 不做云端执行监控

### 风险 6：本地运行时和桌面 UI 耦合过深

控制：

- React 控制台只调用本地 API
- Electron 主进程只做桥接和窗口管理
- 本地运行时独立承担执行和权限校验

## 12. 当前结论

你的理解是正确的：项目需要同时规划云服务管理面板、云端 Registry 后端、桌面客户端和本地运行时。

但优先级不是平均推进。当前工程应优先完成桌面端 MVP 和本地运行时边界；云端先做资源目录、下载、版本和管理后台的最小闭环；不应过早投入全云端执行或移动端完整工作台。

Go Agent Platform 后续最合理的方向是：

**桌面端为核心、本地优先执行、云端统一分发、管理后台统一供给、移动端辅助协同。**
